import Foundation
import SwiftUI

public class BaseRenderer {
    
    public func renderParticle(_ particle: Particle, in context: inout GraphicsContext) {
    }
    
    public func renderCircle(_ particle: Particle, in context: inout GraphicsContext) {
    }
    
    public func renderRectangle(_ particle: Particle, in context: inout GraphicsContext) {
    }
    
    public func renderImage(_ particle: Particle, in context: inout GraphicsContext) {
    }
}
